import { readFile, writeFile, mkdir } from 'node:fs/promises';

async function text(path) { return await readFile(path, 'utf8'); }
async function save(path, value) { await writeFile(path, value.replace(/\r\n/g, '\n'), 'utf8'); }
function replaceOnce(source, before, after, label) {
  const index = source.indexOf(before);
  if (index < 0) throw new Error(`Patch anchor not found: ${label}`);
  if (source.indexOf(before, index + before.length) >= 0) throw new Error(`Patch anchor is ambiguous: ${label}`);
  return source.slice(0, index) + after + source.slice(index + before.length);
}

// 1) Dedicated delete path and custom confirmation UI.
{
  const path = 'src/App.svelte';
  let source = await text(path);
  source = replaceOnce(
    source,
    "  import { cancelOperation, contract, getLiveWorkspace, hideQuickPanel, onOperationEvent, onTrayAction, runOperation } from './lib/bridge';",
    "  import { cancelOperation, contract, deleteCapsule, getLiveWorkspace, hideQuickPanel, markCapsuleDeleted, onOperationEvent, onTrayAction, runOperation } from './lib/bridge';",
    'App bridge imports'
  );
  source = replaceOnce(
    source,
    "  import FullApp from './components/FullApp.svelte';",
    "  import DeleteConfirmModal from './components/DeleteConfirmModal.svelte';\n  import FullApp from './components/FullApp.svelte';",
    'App delete modal import'
  );
  source = replaceOnce(
    source,
    "  let onboardingVisible = $state(localStorage.getItem(ONBOARDING_KEY) !== 'done');",
    "  let onboardingVisible = $state(localStorage.getItem(ONBOARDING_KEY) !== 'done');\n  let pendingDelete = $state<string | null>(null);\n  let deleteBusy = $state(false);\n  let deleteError = $state('');",
    'App delete state'
  );
  source = replaceOnce(
    source,
    "  async function execute(request: OperationRequest) {\n    if (busy) return;\n    if (request.kind === 'delete' && !window.confirm(`Delete “${request.name}” and all of its revisions?`)) return;\n",
    "  async function execute(request: OperationRequest) {\n    if (busy) return;\n    if (request.kind === 'delete') {\n      pendingDelete = request.name;\n      deleteError = '';\n      return;\n    }\n",
    'App native delete confirmation'
  );
  source = replaceOnce(
    source,
    "  async function cancelCurrentSave() {",
    "  async function confirmDelete() {\n    const name = pendingDelete;\n    if (!name || deleteBusy || busy) return;\n\n    deleteBusy = true;\n    deleteError = '';\n    busy = true;\n    try {\n      await deleteCapsule(name);\n      markCapsuleDeleted(name);\n      pendingDelete = null;\n      refreshVersion += 1;\n      void notify('Context Capsule', `Deleted ${name}.`);\n    } catch (error) {\n      deleteError = error instanceof Error ? error.message : String(error);\n    } finally {\n      busy = false;\n      deleteBusy = false;\n    }\n  }\n\n  async function cancelCurrentSave() {",
    'App dedicated delete function'
  );
  source = replaceOnce(
    source,
    "{#if operationVisible}\n  <OperationOverlay",
    "{#if pendingDelete}\n  <DeleteConfirmModal\n    name={pendingDelete}\n    deleting={deleteBusy}\n    error={deleteError}\n    onclose={() => { if (!deleteBusy) { pendingDelete = null; deleteError = ''; } }}\n    onconfirm={confirmDelete}\n  />\n{/if}\n\n{#if operationVisible}\n  <OperationOverlay",
    'App delete modal render'
  );
  await save(path, source);
}

// 2) Keep the visible library refresh instant after the DB operation succeeds.
{
  const path = 'src/lib/bridge.ts';
  let source = await text(path);
  source = replaceOnce(
    source,
    "export const getOverview = () => queryDesktop<OverviewData>('overview');",
    `let overviewCache: OverviewData | null = null;\nlet serveCachedOverviewOnce = false;\n\nexport async function getOverview(): Promise<OverviewData> {\n  if (serveCachedOverviewOnce && overviewCache) {\n    serveCachedOverviewOnce = false;\n    const snapshot = overviewCache;\n    // Reconcile quietly after the UI has already removed the deleted capsule.\n    // A failed background read must not resurrect stale data or block the user.\n    void queryDesktop<OverviewData>('overview')\n      .then((value) => { overviewCache = value; })\n      .catch((error) => traceFrontend('overview.reconcile', \`failed error=\${error instanceof Error ? error.message : String(error)}\`));\n    return snapshot;\n  }\n\n  const value = await queryDesktop<OverviewData>('overview');\n  overviewCache = value;\n  return value;\n}\n\nexport function markCapsuleDeleted(name: string) {\n  if (!overviewCache) return;\n  const normalized = name.trim().toLowerCase();\n  overviewCache = {\n    ...overviewCache,\n    capsules: overviewCache.capsules.filter((capsule) => capsule.name.trim().toLowerCase() !== normalized)\n  };\n  serveCachedOverviewOnce = true;\n}`,
    'bridge overview cache'
  );
  source = replaceOnce(
    source,
    "export const runOperation = (request: OperationRequest) =>\n  invoke<OperationResult>('run_operation', { request });",
    "export const runOperation = (request: OperationRequest) =>\n  invoke<OperationResult>('run_operation', { request });\nexport const deleteCapsule = (name: string) =>\n  invoke<void>('delete_capsule', { name });",
    'bridge dedicated delete invoke'
  );
  await save(path, source);
}

// 3) Add a narrow Tauri delete command: one sidecar process, no operation event stream.
{
  const path = 'src-tauri/src/lib.rs';
  let source = await text(path);
  const anchor = "async fn desktop_api_call(app: &AppHandle, action: &str, args: &[String]) -> Result<Value, String> {";
  const implementation = `#[tauri::command]\nasync fn delete_capsule(app: AppHandle, name: String) -> Result<(), String> {\n    require_nonempty(\"capsule name\", &name)?;\n    let started = Instant::now();\n    append_app_log(format!(\"delete.begin capsule={name:?}\"));\n\n    let (mut rx, _child) = app\n        .shell()\n        .sidecar(\"capsule\")\n        .map_err(|error| format!(\"Context Capsule sidecar is unavailable: {error}\"))?\n        .args([\"delete\".to_owned(), name.clone()])\n        .spawn()\n        .map_err(|error| format!(\"could not start capsule delete: {error}\"))?;\n\n    let mut stderr = String::new();\n    let mut termination_code = None;\n    while let Some(event) = rx.recv().await {\n        match event {\n            CommandEvent::Stderr(bytes) => {\n                push_bounded(&mut stderr, &String::from_utf8_lossy(&bytes));\n            }\n            CommandEvent::Error(error) => {\n                push_bounded(&mut stderr, &error);\n            }\n            CommandEvent::Terminated(payload) => {\n                termination_code = Some(payload.code.unwrap_or(1));\n                break;\n            }\n            _ => {}\n        }\n    }\n\n    let code = termination_code\n        .ok_or_else(|| \"capsule delete ended without a termination event\".to_owned())?;\n    append_app_log(format!(\n        \"delete.complete capsule={name:?} code={code} elapsed_ms={}\",\n        started.elapsed().as_millis()\n    ));\n    if code == 0 {\n        Ok(())\n    } else {\n        let message = stderr.trim();\n        Err(if message.is_empty() {\n            format!(\"capsule delete failed with exit code {code}\")\n        } else {\n            message.to_owned()\n        })\n    }\n}\n\n`;
  source = replaceOnce(source, anchor, implementation + anchor, 'Rust dedicated delete command');
  source = replaceOnce(
    source,
    "            query_desktop,\n            run_operation,",
    "            query_desktop,\n            delete_capsule,\n            run_operation,",
    'Rust invoke handler delete registration'
  );
  await save(path, source);
}

// 4) Replace the prototype sidebar controller with a border-mounted, persistent control.
await save('src/lib/responsive-sidebar.ts', `const SIDEBAR_BREAKPOINT = 820;\nconst SIDEBAR_PREFERENCE_KEY = 'context-capsule:sidebar-expanded:v2';\n\nfunction readWidePreference() {\n  try {\n    const value = localStorage.getItem(SIDEBAR_PREFERENCE_KEY);\n    return value === null ? true : value === 'true';\n  } catch {\n    return true;\n  }\n}\n\nexport function installResponsiveSidebar() {\n  const root = document.querySelector<HTMLElement>('.full-root');\n  const sidebar = root?.querySelector<HTMLElement>('.sidebar');\n  if (!root || !sidebar) return () => undefined;\n\n  const media = window.matchMedia(\`(max-width: \${SIDEBAR_BREAKPOINT}px)\`);\n  let expanded = media.matches ? false : readWidePreference();\n  let manualNarrowChoice: boolean | null = null;\n\n  const toggle = document.createElement('button');\n  toggle.type = 'button';\n  toggle.className = 'sidebar-toggle';\n  toggle.dataset.sidebarToggle = 'true';\n  toggle.innerHTML = '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m14.5 6.5-5 5.5 5 5.5"/></svg>';
  sidebar.append(toggle);\n\n  for (const button of sidebar.querySelectorAll<HTMLButtonElement>('nav button')) {\n    const label = button.textContent?.trim();\n    if (label) button.dataset.sidebarLabel = label;\n  }\n\n  const apply = () => {\n    root.classList.toggle('sidebar-collapsed', !expanded);\n    sidebar.dataset.state = expanded ? 'expanded' : 'collapsed';\n    toggle.dataset.state = expanded ? 'expanded' : 'collapsed';\n    toggle.setAttribute('aria-expanded', String(expanded));\n    const label = expanded ? 'Collapse sidebar' : 'Expand sidebar';\n    toggle.setAttribute('aria-label', label);\n    toggle.title = label;\n  };\n\n  const onToggle = () => {\n    expanded = !expanded;\n    if (media.matches) {\n      manualNarrowChoice = expanded;\n    } else {\n      try { localStorage.setItem(SIDEBAR_PREFERENCE_KEY, String(expanded)); } catch {}\n    }\n    apply();\n  };\n\n  const onBreakpoint = (event: MediaQueryListEvent) => {\n    expanded = event.matches ? (manualNarrowChoice ?? false) : readWidePreference();\n    apply();\n  };\n\n  toggle.addEventListener('click', onToggle);\n  media.addEventListener('change', onBreakpoint);\n  apply();\n\n  return () => {\n    media.removeEventListener('change', onBreakpoint);\n    toggle.removeEventListener('click', onToggle);\n    toggle.remove();\n    root.classList.remove('sidebar-collapsed');\n    delete sidebar.dataset.state;\n  };\n}\n\nexport { SIDEBAR_BREAKPOINT };\n`);

await save('src/responsive-sidebar.css', `.full-root {\n  --sidebar-expanded-width: 224px;\n  --sidebar-collapsed-width: 72px;\n  --sidebar-current-width: var(--sidebar-expanded-width);\n  grid-template-columns: var(--sidebar-current-width) minmax(0, 1fr);\n  transition: grid-template-columns .28s cubic-bezier(.22, .9, .32, 1);\n}\n\n.full-root.sidebar-collapsed { --sidebar-current-width: var(--sidebar-collapsed-width); }\n.full-content { min-width: 0; transition: padding .22s ease; }\n.sidebar {\n  position: relative;\n  min-width: 0;\n  overflow: visible;\n  transition: padding .24s cubic-bezier(.22, .9, .32, 1);\n}\n.sidebar::after {\n  content: '';\n  position: absolute;\n  top: 18px;\n  bottom: 18px;\n  right: 0;\n  width: 1px;\n  background: linear-gradient(180deg, transparent, rgba(255,255,255,.1) 15%, rgba(255,255,255,.06) 82%, transparent);\n  pointer-events: none;\n}\n.sidebar .full-brand { min-width: 0; }\n.sidebar .full-brand > div:nth-child(2) {\n  min-width: 0;\n  white-space: nowrap;\n  overflow: hidden;\n  opacity: 1;\n  transform: translateX(0);\n  transition: opacity .16s ease, transform .22s ease, width .22s ease;\n}\n\n.sidebar-toggle {\n  position: absolute;\n  z-index: 30;\n  top: 76px;\n  right: -15px;\n  width: 30px;\n  height: 30px;\n  display: grid;\n  place-items: center;\n  padding: 0;\n  border: 1px solid rgba(255,255,255,.12);\n  border-radius: 999px;\n  background: rgba(20, 23, 29, .82);\n  box-shadow: 0 9px 28px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.07);\n  color: var(--muted);\n  backdrop-filter: blur(18px) saturate(135%);\n  -webkit-backdrop-filter: blur(18px) saturate(135%);\n  cursor: pointer;\n  transition: color .16s ease, border-color .16s ease, background .16s ease, box-shadow .16s ease, transform .16s ease;\n}\n.sidebar-toggle:hover {\n  color: var(--ink);\n  border-color: rgba(234,255,54,.3);\n  background: rgba(28,32,39,.94);\n  box-shadow: 0 10px 30px rgba(0,0,0,.34), 0 0 0 3px rgba(234,255,54,.045);\n  transform: scale(1.055);\n}\n.sidebar-toggle:focus-visible { outline: 2px solid rgba(234,255,54,.75); outline-offset: 3px; }\n.sidebar-toggle svg {\n  width: 16px;\n  height: 16px;\n  fill: none;\n  stroke: currentColor;\n  stroke-width: 2;\n  stroke-linecap: round;\n  stroke-linejoin: round;\n  transition: transform .24s cubic-bezier(.22, .9, .32, 1);\n}\n.sidebar-toggle[data-state='collapsed'] svg { transform: rotate(180deg); }\n\n.full-root.sidebar-collapsed .sidebar { padding-left: 10px; padding-right: 10px; }\n.full-root.sidebar-collapsed .full-brand { justify-content: center; padding-left: 0; padding-right: 0; }\n.full-root.sidebar-collapsed .full-brand > div:nth-child(2) {\n  width: 0;\n  opacity: 0;\n  transform: translateX(-8px);\n  pointer-events: none;\n}\n.full-root.sidebar-collapsed .full-brand .brand-mark { flex: 0 0 auto; margin: 0; }\n.full-root.sidebar-collapsed .sidebar nav button {\n  position: relative;\n  width: 44px;\n  min-width: 44px;\n  margin-inline: auto;\n  justify-content: center;\n  gap: 0;\n  padding-left: 0;\n  padding-right: 0;\n  font-size: 0;\n}\n.full-root.sidebar-collapsed .sidebar nav button svg { flex: 0 0 auto; }\n.full-root.sidebar-collapsed .sidebar nav button::after {\n  content: attr(data-sidebar-label);\n  position: absolute;\n  left: calc(100% + 12px);\n  top: 50%;\n  transform: translate(4px, -50%);\n  padding: 7px 9px;\n  border: 1px solid rgba(255,255,255,.09);\n  border-radius: 9px;\n  background: rgba(18,21,27,.94);\n  box-shadow: 0 8px 28px rgba(0,0,0,.28);\n  color: var(--ink);\n  font-size: 12px;\n  font-weight: 600;\n  line-height: 1;\n  white-space: nowrap;\n  opacity: 0;\n  pointer-events: none;\n  transition: opacity .14s ease, transform .14s ease;\n}\n.full-root.sidebar-collapsed .sidebar nav button:hover::after,\n.full-root.sidebar-collapsed .sidebar nav button:focus-visible::after { opacity: 1; transform: translate(0, -50%); }\n.full-root.sidebar-collapsed .sidebar-status { align-items: center; padding-left: 0; padding-right: 0; }\n.full-root.sidebar-collapsed .sidebar-status > span,\n.full-root.sidebar-collapsed .sidebar-status .status-pill > span:last-child { display: none; }\n.full-root.sidebar-collapsed .sidebar-status .status-pill { align-self: center; padding: 6px; }\n\n@media (max-width: 820px) {\n  .full-content { padding: 20px 16px 52px; }\n  .capsule-grid { grid-template-columns: minmax(0, 1fr); }\n  .library-toolbar { gap: 8px; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .full-root, .full-content, .sidebar, .sidebar-toggle, .sidebar-toggle svg, .sidebar .full-brand > div:nth-child(2) { transition: none !important; }\n}\n`);

// 5) App-native delete modal.
await save('src/components/DeleteConfirmModal.svelte', `<script lang="ts">\n  import { LoaderCircle, Trash2, TriangleAlert } from '@lucide/svelte';\n  import Modal from './Modal.svelte';\n\n  let { name, deleting = false, error = '', onclose, onconfirm } = $props<{\n    name: string;\n    deleting?: boolean;\n    error?: string;\n    onclose: () => void;\n    onconfirm: () => void;\n  }>();\n\n  const close = () => { if (!deleting) onclose(); };\n</script>\n\n<Modal title="Delete capsule?" subtitle="This action cannot be undone." onclose={close} width={440}>\n  <div class="delete-confirm">\n    <div class="delete-confirm-warning">\n      <div class="delete-confirm-icon"><TriangleAlert size={19}/></div>\n      <div>\n        <strong>Remove “{name}” permanently?</strong>\n        <p>All saved revisions for this capsule will be removed from local Context Capsule storage. Your project files are not touched.</p>\n      </div>\n    </div>\n\n    {#if error}\n      <div class="delete-confirm-error" role="alert">{error}</div>\n    {/if}\n\n    <div class="modal-actions delete-confirm-actions">\n      <button class="secondary-button" disabled={deleting} onclick={close}>Cancel</button>\n      <button class="danger-button delete-confirm-submit" disabled={deleting} onclick={onconfirm}>\n        {#if deleting}<LoaderCircle size={15} class="spin"/> Deleting…{:else}<Trash2 size={15}/> Delete capsule{/if}\n      </button>\n    </div>\n  </div>\n</Modal>\n`);

await save('src/delete-confirm.css', `.delete-confirm { display: grid; gap: 18px; }\n.delete-confirm-warning {\n  display: grid;\n  grid-template-columns: 42px minmax(0, 1fr);\n  gap: 12px;\n  align-items: start;\n  padding: 14px;\n  border: 1px solid rgba(255, 103, 103, .16);\n  border-radius: 16px;\n  background: rgba(255, 83, 83, .055);\n}\n.delete-confirm-icon {\n  width: 38px;\n  height: 38px;\n  display: grid;\n  place-items: center;\n  border-radius: 12px;\n  background: rgba(255, 91, 91, .11);\n  color: #ff8f8f;\n}\n.delete-confirm-warning strong { display: block; margin: 1px 0 5px; font-size: 14px; color: var(--ink); }\n.delete-confirm-warning p { margin: 0; color: var(--muted); font-size: 12.5px; line-height: 1.55; }\n.delete-confirm-error {\n  padding: 10px 12px;\n  border: 1px solid rgba(255, 103, 103, .2);\n  border-radius: 12px;\n  background: rgba(255, 83, 83, .07);\n  color: #ffb0b0;\n  font-size: 12px;\n  line-height: 1.45;\n}\n.delete-confirm-actions { margin-top: 0; }\n.delete-confirm-submit { min-width: 142px; justify-content: center; }\n.delete-confirm-submit:disabled { opacity: .62; cursor: default; }\n`);

{
  const path = 'src/main.ts';
  let source = await text(path);
  source = replaceOnce(
    source,
    "import './full-save-scroll.css';\nimport './responsive-sidebar.css';",
    "import './full-save-scroll.css';\nimport './responsive-sidebar.css';\nimport './delete-confirm.css';",
    'main delete modal stylesheet'
  );
  await save(path, source);
}

// 6) Regression tests for behavior and design contracts.
await save('tests/delete-responsive-tray.test.mjs', `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { readFile } from 'node:fs/promises';\nimport { resolve } from 'node:path';\n\nconst root = resolve(import.meta.dirname, '..');\nconst read = (path) => readFile(resolve(root, path), 'utf8');\n\ntest('generic operations still finish on direct child termination', async () => {\n  const source = await read('src-tauri/src/lib.rs');\n  const start = source.indexOf('async fn run_operation');\n  const end = source.indexOf('async fn cancel_operation', start);\n  const implementation = source.slice(start, end);\n  assert.match(implementation, /CommandEvent::Terminated\\(payload\\)/);\n  assert.match(implementation, /operation\\.terminated/);\n  const terminated = implementation.slice(implementation.indexOf('CommandEvent::Terminated'));\n  assert.match(terminated.slice(0, terminated.indexOf('_ => {}')), /break;/);\n});\n\ntest('delete has a dedicated narrow Tauri path with authoritative termination', async () => {\n  const rust = await read('src-tauri/src/lib.rs');\n  const start = rust.indexOf('async fn delete_capsule');\n  const end = rust.indexOf('async fn desktop_api_call', start);\n  const implementation = rust.slice(start, end);\n  assert.ok(start >= 0);\n  assert.match(implementation, /sidecar\\(\"capsule\"\\)/);\n  assert.match(implementation, /\\.args\\(\\[\"delete\"\\.to_owned\\(\\), name\\.clone\\(\\)\\]\\)/);\n  assert.doesNotMatch(implementation, /emit_operation|preferred_operation_directory|capsule_exists/);\n  assert.match(implementation, /CommandEvent::Terminated[\\s\\S]*break;/);\n});\n\ntest('delete bypasses window.confirm and the generic operation overlay', async () => {\n  const app = await read('src/App.svelte');\n  const bridge = await read('src/lib/bridge.ts');\n  assert.doesNotMatch(app, /window\\.confirm/);\n  assert.match(app, /pendingDelete = request\\.name/);\n  assert.match(app, /await deleteCapsule\\(name\\)/);\n  assert.match(app, /markCapsuleDeleted\\(name\\)/);\n  assert.match(app, /<DeleteConfirmModal/);\n  assert.match(bridge, /invoke<void>\\('delete_capsule'/);\n});\n\ntest('post-delete overview refresh can paint from the already-loaded cache', async () => {\n  const bridge = await read('src/lib/bridge.ts');\n  assert.match(bridge, /let overviewCache: OverviewData \\| null = null/);\n  assert.match(bridge, /serveCachedOverviewOnce/);\n  assert.match(bridge, /capsules: overviewCache\\.capsules\\.filter/);\n  assert.match(bridge, /overview\\.reconcile/);\n});\n\ntest('delete confirmation is app-native and destructive-action styled', async () => {\n  const modal = await read('src/components/DeleteConfirmModal.svelte');\n  const css = await read('src/delete-confirm.css');\n  assert.match(modal, /<Modal title=\"Delete capsule\\?\"/);\n  assert.match(modal, /Your project files are not touched/);\n  assert.match(modal, /danger-button delete-confirm-submit/);\n  assert.match(css, /delete-confirm-warning/);\n  assert.match(css, /rgba\\(255, 83, 83/);\n});\n\ntest('sidebar toggle sits on the sidebar border and preserves a compact rail', async () => {\n  const controller = await read('src/lib/responsive-sidebar.ts');\n  const css = await read('src/responsive-sidebar.css');\n  const config = JSON.parse(await read('src-tauri/tauri.conf.json'));\n  const mainWindow = config.app.windows.find((window) => window.label === 'main');\n\n  assert.match(controller, /SIDEBAR_BREAKPOINT = 820/);\n  assert.match(controller, /sidebar\\.append\\(toggle\\)/);\n  assert.match(controller, /matchMedia/);\n  assert.match(controller, /SIDEBAR_PREFERENCE_KEY/);\n  assert.match(css, /--sidebar-collapsed-width: 72px/);\n  assert.match(css, /position: absolute;[\\s\\S]*right: -15px/);\n  assert.match(css, /sidebar-toggle\\[data-state='collapsed'\\] svg/);\n  assert.match(css, /data-sidebar-label/);\n  assert.ok(mainWindow.minWidth < 820);\n});\n\ntest('quick WebView backing surface remains explicitly alpha-zero', async () => {\n  const config = JSON.parse(await read('src-tauri/tauri.conf.json'));\n  const quick = config.app.windows.find((window) => window.label === 'quick');\n  assert.equal(quick.transparent, true);\n  assert.equal(quick.backgroundColor, '#00000000');\n  assert.equal(quick.shadow, false);\n  assert.equal(quick.windowEffects, undefined);\n});\n`);

// 7) Lightweight real-CSS visual fixture for the runner screenshots.
await mkdir('tests/visual', { recursive: true });
await save('tests/visual/delete-sidebar.html', `<!doctype html>\n<html data-window-mode="full"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">\n<link rel="stylesheet" href="../../src/app.css"><link rel="stylesheet" href="../../src/glass-overrides.css"><link rel="stylesheet" href="../../src/responsive-sidebar.css"><link rel="stylesheet" href="../../src/delete-confirm.css">\n<style>html,body,#fixture{width:100%;height:100%;margin:0}.fixture-main{padding:34px}.fixture-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:24px}.fixture-card{min-height:150px;border:1px solid rgba(255,255,255,.07);border-radius:20px;background:rgba(255,255,255,.035);padding:18px}.fixture-glass{border:1px solid rgba(255,255,255,.09);border-radius:28px;background:rgba(22,25,31,.93);box-shadow:0 24px 70px rgba(0,0,0,.42)}.fixture-mark{width:38px;height:38px;display:grid;place-items:center;border-radius:12px;background:rgba(234,255,54,.09);color:var(--accent)}.fixture-icon{width:18px;height:18px;border:1.5px solid currentColor;border-radius:5px;opacity:.8}.modal-backdrop[hidden]{display:none!important}</style></head>\n<body><div id="fixture" class="full-root"><aside class="sidebar"><div class="full-brand"><div class="fixture-mark">✦</div><div><strong>Context Capsule</strong><span>Workspace memory</span></div></div><nav>\n<button class="active" data-sidebar-label="Capsules"><span class="fixture-icon"></span> Capsules</button><button data-sidebar-label="Live Workspace"><span class="fixture-icon"></span> Live Workspace</button><button data-sidebar-label="Services"><span class="fixture-icon"></span> Services</button><button data-sidebar-label="System Health"><span class="fixture-icon"></span> System Health</button><button data-sidebar-label="Settings"><span class="fixture-icon"></span> Settings</button></nav><div class="sidebar-status"><span>● System ready</span><span>CLI-backed · API v1</span></div><button class="sidebar-toggle" data-sidebar-toggle="true" data-state="expanded" aria-label="Collapse sidebar"><svg viewBox="0 0 24 24"><path d="m14.5 6.5-5 5.5 5 5.5"/></svg></button></aside>\n<main class="full-content fixture-main"><span class="section-kicker">Library</span><h1>Your Capsules</h1><p>Save whole development contexts and return to them without rebuilding your mental state.</p><div class="fixture-grid"><div class="fixture-card"><h2>Project Atlas</h2><p>4 applications · 8 tabs · 2 terminals</p></div><div class="fixture-card"><h2>Research Notes</h2><p>3 applications · 6 tabs · 1 terminal</p></div></div></main></div>\n<div id="delete-modal" class="modal-backdrop" hidden><div class="modal-shell" style="max-width:440px"><div class="fixture-glass"><div class="modal-content"><div class="modal-heading"><div><h2>Delete capsule?</h2><p>This action cannot be undone.</p></div><button class="icon-button">×</button></div><div class="modal-scroll"><div class="delete-confirm"><div class="delete-confirm-warning"><div class="delete-confirm-icon">!</div><div><strong>Remove “Project Atlas” permanently?</strong><p>All saved revisions for this capsule will be removed from local Context Capsule storage. Your project files are not touched.</p></div></div><div class="modal-actions delete-confirm-actions"><button class="secondary-button">Cancel</button><button class="danger-button delete-confirm-submit">Delete capsule</button></div></div></div></div></div></div></div>\n<script>const params=new URLSearchParams(location.search);const root=document.getElementById('fixture');const toggle=document.querySelector('.sidebar-toggle');if(params.get('state')==='collapsed'){root.classList.add('sidebar-collapsed');toggle.dataset.state='collapsed';toggle.setAttribute('aria-label','Expand sidebar')}if(params.get('dialog')==='1')document.getElementById('delete-modal').hidden=false;</script></body></html>\n`);

console.log('Applied dedicated delete flow, custom confirmation modal, polished responsive sidebar, tests, and visual fixture.');
